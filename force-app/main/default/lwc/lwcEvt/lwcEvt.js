import { LightningElement } from 'lwc';

export default class LwcEvt extends LightningElement {

/*
Aura - evt
Component Evt - we have prent and child
application Evt - one application style(fier evt)=>cmp,ord.cart,template(handle evt)

//Lwc
 const evt = new CustomeEvent('abc');
 this.dispatchEvent(evt); //fire aura
*/

}